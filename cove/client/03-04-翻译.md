---
title: 翻译
---

# 翻译文档

> 支持多语种翻译，译文与原文对照显示，方便逐条确认。

## 多语种翻译

1. 打开一篇需要翻译的文档
2. 点击任务类型中的「翻译」
3. 选择目标语言（如「中文翻译为英文」）
4. 等待处理完成，文档中会生成对照译文

![选择指令](./images/翻译-选择指令.png) ![对照结果](./images/翻译-对照结果.png)

## 视频教程

<div class="cove-video-player" style="margin:12px 0;">
  <div class="cove-video-placeholder" data-video-url="https://github.com/cove-apps/product-manual/releases/download/video-tutorials/06-translate.mp4" onclick="playCoveVideo(this)" style="cursor:pointer;padding:24px;background:#f0f4ff;border:1px solid #dbe5ff;border-radius:12px;text-align:center;transition:background 0.2s;">
    <div style="font-size:40px;margin-bottom:8px;">▶️</div>
    <div style="font-size:14px;color:#2563eb;font-weight:500;">翻译演示</div>
    <div style="font-size:12px;color:#999;margin-top:4px;">首次加载需等待几秒</div>
  </div>
</div>
<script>
function playCoveVideo(el) {
  var container = el.parentNode;
  var url = el.getAttribute('data-video-url');
  if (!url) return;
  el.innerHTML = '<div style="padding:20px;color:#666;">加载中...</div>';
  el.style.cursor = 'default';
  el.onclick = null;
  var xhr = new XMLHttpRequest();
  xhr.open('GET', url, true);
  xhr.responseType = 'blob';
  xhr.onload = function() {
    if (xhr.status === 200) {
      var video = document.createElement('video');
      video.controls = true;
      video.style.width = '100%';
      video.style.maxWidth = '720px';
      video.style.borderRadius = '8px';
      video.style.display = 'block';
      video.src = URL.createObjectURL(xhr.response);
      container.innerHTML = '';
      container.appendChild(video);
      video.play().catch(function(){});
    } else {
      el.innerHTML = '<div style="color:red;padding:20px;">加载失败，请重试</div>';
    }
  };
  xhr.onerror = function() {
    el.innerHTML = '<div style="color:red;padding:20px;">网络错误，请重试</div>';
  };
  xhr.send();
}
</script>
</a>
</div>
翻译结果以颜色区分：
- **原文**保持原有颜色
- **译文**以蓝色显示，与原文段落对照排列
- 确认弹窗中可逐条或全部确认

## 新增翻译语种

如果需要的语种不在内置列表中：

1. 点击快捷指令旁的「+」
2. 输入自然语言描述，如「将文档内容翻译成葡萄牙语」
3. 保存为快捷指令，方便后续调用

## 技术说明

- 支持中英、英中及其他常见语种
- 支持 RTL（从右到左）语言
- 支持选区翻译和全文翻译两种模式
- 任务中途取消可整篇恢复到原文状态
