---
title: 校对
---

# 校对文档

> 用 AI 找出文档中的错别字、语法错误等并进行纠正。

## 常规纠错

1. 打开一篇需要校对的文档
2. 点击任务类型中的「校对」
3. 选择「常规纠错」快捷指令
4. 等待处理完成，弹出确认窗口

![操作前](./images/校对-操作前.png) ![结果弹窗](./images/校对-结果弹窗.png)

## 视频教程

<div class="cove-video-player" style="margin:12px 0;">
  <div class="cove-video-placeholder" data-video-url="https://github.com/cove-apps/product-manual/releases/download/video-tutorials/02-proofread.mp4" onclick="playCoveVideo(this)" style="cursor:pointer;padding:24px;background:#f0f4ff;border:1px solid #dbe5ff;border-radius:12px;text-align:center;transition:background 0.2s;">
    <div style="font-size:40px;margin-bottom:8px;">▶️</div>
    <div style="font-size:14px;color:#2563eb;font-weight:500;">基础校对演示</div>
    <div style="font-size:12px;color:#999;margin-top:4px;">首次加载需等待几秒</div>
  </div>
</div>
<script>
function playCoveVideo(el) {
  var container = el.parentNode;
  var url = el.getAttribute('data-video-url');
  if (!url) return;
  el.innerHTML = '<div style="padding:20px;color:#666;">加载中...</div>';
  el.style.cursor = 'default';
  el.onclick = null;
  var xhr = new XMLHttpRequest();
  xhr.open('GET', url, true);
  xhr.responseType = 'blob';
  xhr.onload = function() {
    if (xhr.status === 200) {
      var video = document.createElement('video');
      video.controls = true;
      video.style.width = '100%';
      video.style.maxWidth = '720px';
      video.style.borderRadius = '8px';
      video.style.display = 'block';
      video.src = URL.createObjectURL(xhr.response);
      container.innerHTML = '';
      container.appendChild(video);
      video.play().catch(function(){});
    } else {
      el.innerHTML = '<div style="color:red;padding:20px;">加载失败，请重试</div>';
    }
  };
  xhr.onerror = function() {
    el.innerHTML = '<div style="color:red;padding:20px;">网络错误，请重试</div>';
  };
  xhr.send();
}
</script>
</a>
</div>
## 其他校对功能

内置的其他校对快捷指令包括：

| 功能 | 说明 |
|------|------|
| **公文写作规范** | 针对党政公文格式的专项校对 |
| **技术文档校对** | 面向技术文档的专业校对 |
| **领导人职务及排序** | 检查领导人姓名、职务、排序规范 |
| **国家标准引用校对** | 识别国家标准引用错误（编号格式、名称错配、标准过期等） |

## 关于快捷指令

- 初始快捷指令较为通用，管理员可在后台随时调整
- 你可以点击「+」新增个人快捷指令，保存常用需求

> 复杂校对需求可联系服务顾问定制。
