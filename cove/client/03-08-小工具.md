---
title: 小工具
---

# 小工具

> 处理各种小而繁琐的文档操作，一键完成。

## 删除多余空行

1. 打开需要清理的文档
2. 点击「小工具」→「删除多余空行」
3. AI 自动识别并删除多余空行

![删除空行](./images/小工具-删除空行.png) ![已删除](./images/小工具-空行已删除.png)

## 视频教程

<div class="cove-video-player" style="margin:12px 0;">
  <div class="cove-video-placeholder" data-video-url="https://github.com/cove-apps/product-manual/releases/download/video-tutorials/09-clear-format.mp4" onclick="playCoveVideo(this)" style="cursor:pointer;padding:24px;background:#f0f4ff;border:1px solid #dbe5ff;border-radius:12px;text-align:center;transition:background 0.2s;">
    <div style="font-size:40px;margin-bottom:8px;">▶️</div>
    <div style="font-size:14px;color:#2563eb;font-weight:500;">清除格式演示</div>
    <div style="font-size:12px;color:#999;margin-top:4px;">首次加载需等待几秒</div>
  </div>
</div>
<script>
function playCoveVideo(el) {
  var container = el.parentNode;
  var url = el.getAttribute('data-video-url');
  if (!url) return;
  el.innerHTML = '<div style="padding:20px;color:#666;">加载中...</div>';
  el.style.cursor = 'default';
  el.onclick = null;
  var xhr = new XMLHttpRequest();
  xhr.open('GET', url, true);
  xhr.responseType = 'blob';
  xhr.onload = function() {
    if (xhr.status === 200) {
      var video = document.createElement('video');
      video.controls = true;
      video.style.width = '100%';
      video.style.maxWidth = '720px';
      video.style.borderRadius = '8px';
      video.style.display = 'block';
      video.src = URL.createObjectURL(xhr.response);
      container.innerHTML = '';
      container.appendChild(video);
      video.play().catch(function(){});
    } else {
      el.innerHTML = '<div style="color:red;padding:20px;">加载失败，请重试</div>';
    }
  };
  xhr.onerror = function() {
    el.innerHTML = '<div style="color:red;padding:20px;">网络错误，请重试</div>';
  };
  xhr.send();
}
</script>
<div class="cove-video-player" style="margin:12px 0;">
  <div class="cove-video-placeholder" data-video-url="https://github.com/cove-apps/product-manual/releases/download/video-tutorials/10-delete-comments.mp4" onclick="playCoveVideo(this)" style="cursor:pointer;padding:24px;background:#f0f4ff;border:1px solid #dbe5ff;border-radius:12px;text-align:center;transition:background 0.2s;">
    <div style="font-size:40px;margin-bottom:8px;">▶️</div>
    <div style="font-size:14px;color:#2563eb;font-weight:500;">删除批注演示</div>
    <div style="font-size:12px;color:#999;margin-top:4px;">首次加载需等待几秒</div>
  </div>
</div>
<script>
function playCoveVideo(el) {
  var container = el.parentNode;
  var url = el.getAttribute('data-video-url');
  if (!url) return;
  el.innerHTML = '<div style="padding:20px;color:#666;">加载中...</div>';
  el.style.cursor = 'default';
  el.onclick = null;
  var xhr = new XMLHttpRequest();
  xhr.open('GET', url, true);
  xhr.responseType = 'blob';
  xhr.onload = function() {
    if (xhr.status === 200) {
      var video = document.createElement('video');
      video.controls = true;
      video.style.width = '100%';
      video.style.maxWidth = '720px';
      video.style.borderRadius = '8px';
      video.style.display = 'block';
      video.src = URL.createObjectURL(xhr.response);
      container.innerHTML = '';
      container.appendChild(video);
      video.play().catch(function(){});
    } else {
      el.innerHTML = '<div style="color:red;padding:20px;">加载失败，请重试</div>';
    }
  };
  xhr.onerror = function() {
    el.innerHTML = '<div style="color:red;padding:20px;">网络错误，请重试</div>';
  };
  xhr.send();
}
</script>
</a>
</div>
<div style="display:inline-block;margin:8px 8px 8px 0;padding:16px 20px;background:#f8faff;border:1px solid #dbe5ff;border-radius:10px;">
  <div style="font-size:24px;margin-bottom:6px;">▶️</div>
  <div style="font-size:14px;font-weight:500;color:#2563eb;margin-bottom:8px;">删除批注演示</div>
  <a href="https://github.com/cove-apps/product-manual/releases/download/video-tutorials/10-delete-comments.mp4" target="_blank" style="display:inline-block;padding:6px 14px;background:#2563eb;color:#fff;border-radius:6px;text-decoration:none;font-size:13px;">📥 下载视频</a>
</div>
<video src="https://github.com/cove-apps/product-manual/releases/download/video-tutorials/10-delete-comments.mp4" controls width="100%" style="max-width:720px;border-radius:8px;">
  您的浏览器不支持视频播放，请下载后观看。
</video>

> 📺 视频教程（稍后上线）

## 表格统一对齐

1. 打开包含表格的文档
2. 点击「小工具」→「表格统一」
3. 自动调整文档中所有表格的对齐方式

![表格对齐](./images/小工具-表格对齐.png) ![已对齐](./images/小工具-表格已对齐.png)

## 其他小工具

| 功能 | 说明 |
|------|------|
| **一键清除格式** | 清除文档中所有多余格式 |
| **一键删除批注** | 删除文档中的所有批注 |
| **文档清理** | 按规则批量删除英文段落、URL、邮箱、空行等 |

> 小工具可按你的日常需求自定义增加，尝试用自然语言描述你的需求即可。
