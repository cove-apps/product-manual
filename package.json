{
  "name": "product-manual",
  "version": "1.0.0",
  "description": "产品手册自动发布系统",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "generate:changelog": "node scripts/generate-changelog.js",
    "export:pdf": "node scripts/export-pdf.cjs",
    "deploy": "npm run build && npm run export:pdf"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "vitepress": "^1.6.4",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "puppeteer": "^24.43.1"
  }
}
